'"Типы данных"'

# print("Hello")

'"print(hello)"'

# 1. NoneType -> Ничего, пустота
# 2. Boolean -> Правда или Ложь (True/False)
# 3. Числовые типы данных:
    # a. integer -> int() Целые числа (1,2,3,4)
    # b. float() -> Числа с плавающей точкой (1.2 , 4.9, 4.0, 12.901)
    # c. complex(3, 5) -> 3+5j Комплексные числа
# 4. Список (массив) -. list() [1, 2, 3, 1.2, None, True, False ...]
# 5. кортеж -> tuple() (1, 2, 3, None, True ..)
# 6. Строки str() -> "Hello78909-4", """ """, "", ''' '''
# 7. Множество -> set() {1, 2, 3, None, False}
# 8. frozenset() -> Замораженное множество
# 9. Словари -> dict() содержит данные в виде ключа: значение -> {1: 'one', 2: "two"}
# Immutable (неизменяемые типы данных):
     # 1. NoneType (None)
     # 2. Boolean (True/False)
     # 3. int()
     # 4. float()
     # 5. compleх()
     # 6. tuple()
     # 7. str()
     # 8. frozenset()
# Mutable (изменяемые типы данных)
     # 1. list()
     # 2. set()
     # 3. dict() 

'''Переменные'''
# это ссылка на ячейку в памяти в которой хранится какая-то инфориация. Предназначены для хранения данных

acer = 'hello' 
number = '5467890' # тип данных str
hello = 88 # int
a = 9
_hello = 'можно'
hello_word = 'можно'
hello3 = 'hello hello hello'
hello_3 = '888'
Me = 89 # нельзя

print = 5 # нельзя использовать зарезервированные слова
# int(), float() str() ...
print_ = 5 # можно
 

'''=== Ввод и вывод данных==='''
# print() -> фунеция стандартного вывода данных
# input() ->  функция стандартного вывода данных (str)

name = input('Введите имя:')
print(type(name))
# int(type(name))
 # type() -> возвращает тип данных 

 # int() ->  переводит в тип данный целые числа


# str_num = '1234567'
# print(type(str_num))


# print(3+3)
#num = int(input('шаблон'))
#result = num ** 2
#print(result)
# a = 5
# b = 10
# c = 15
# print(a, b, c)






     